using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundManager : MonoBehaviour
{
    public AudioSource musicPlayer, sfxPlayer;
    public AudioClip musicBattle, musicBoss, musicDefeat, musicLevel, musicMenu, musicVictory, musicShovel;
    public AudioClip soundButton, soundClang, soundDash, soundGuard, soundHurt, soundItem, soundKaching, soundLandHeavy, soundLandMedium, soundPJump, soundPLand, soundSpellCharge, soundSpellUse, soundExplode, soundShoot;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
